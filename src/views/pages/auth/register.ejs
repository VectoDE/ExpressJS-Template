<!DOCTYPE html>
<html lang="de">
  <head>
    <%- include('../../partials/head', { title }) %>
  </head>
  <body class="page-auth">
    <%- include('../../partials/header') %>
    <main class="container narrow">
      <%- include('../../partials/flash') %>
      <section class="card">
        <h1>Registrierung</h1>
        <p>Lege ein gesichertes Konto an, um sofort mit API und Dashboard zu starten.</p>
        <form method="post" action="/register" class="form-grid">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <label for="name">Name</label>
          <input type="text" id="name" name="name" minlength="3" maxlength="120" autocomplete="name" required />

          <label for="email">E-Mail-Adresse</label>
          <input type="email" id="email" name="email" autocomplete="email" required />

          <label for="password">Passwort</label>
          <input
            type="password"
            id="password"
            name="password"
            minlength="12"
            autocomplete="new-password"
            required
            aria-describedby="password-help"
          />
          <p id="password-help" class="form-help">
            Mindestens 12 Zeichen sowie Groß-/Kleinschreibung, Zahl und Sonderzeichen.
          </p>

          <button type="submit" class="button primary">Konto erstellen</button>
        </form>
        <p class="form-footer">Bereits registriert? <a href="/login">Zur Anmeldung</a>.</p>
      </section>
    </main>
    <%- include('../../partials/footer') %>
    <script src="/assets/js/main.js" defer></script>
  </body>
</html>
